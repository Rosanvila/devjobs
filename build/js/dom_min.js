const filterBtn=document.querySelector(".filter-button");const mobileModal=document.querySelector("#modal");const overlay=document.querySelector(".overlay");filterBtn.addEventListener("click",(()=>{mobileModal.classList.add("modal-open");overlay.classList.add("overlay-open")}));if(window.innerWidth>768){overlay.classList.remove("overlay-open")}overlay.addEventListener("click",(()=>{mobileModal.classList.remove("modal-open");overlay.classList.remove("overlay-open")}));const jobsContainer=document.querySelector("#jobs-container");const jobsTemplate=document.querySelector("#job-template");createCardElement=job=>{const clonedCard=jobsTemplate.content.cloneNode(true);const jobCompany=clonedCard.querySelector("#company");const jobContract=clonedCard.querySelector("#contract");const jobLocation=clonedCard.querySelector(".job-location");const jobPosition=clonedCard.querySelector("#position");const jobPostedAt=clonedCard.querySelector("#postedAt");const jobTitleLink=clonedCard.querySelector(".job-title");jobTitleLink.href=`job-detail.html?id=${job.id}`;const jobLogo=clonedCard.querySelector(".logo");jobLogo.src="https://ecf-dwwm.cefim-formation.org"+job.logo;jobLogo.style.backgroundColor=job.logoBackground;jobCompany.textContent=`${job.company}`;jobContract.textContent=`${job.contract}`;jobLocation.textContent=`${job.location}`;jobPosition.textContent=`${job.position}`;jobPostedAt.textContent=timeFormat(job.postedAt);return clonedCard};const jobsCard=job=>{const cardElement=createCardElement(job);jobsContainer.appendChild(cardElement)};timeFormat=timestamp=>{const now=Date.now();const difference=now-timestamp;const days=Math.floor(difference/(1e3*60*60*24));if(days<1){const hours=Math.floor(difference/(1e3*60*60));return hours+"h ago"}else if(days<7){return days+"d ago"}else if(days<30){const weeks=Math.floor(days/7);return weeks+"w ago"}else if(days<365){const months=Math.floor(days/30);return months+"mo ago"}else{const years=Math.floor(days/365);return years+"y ago"}};